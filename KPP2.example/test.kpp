class PlaneController {
	bool isTakingOff = false;

	unit TakeOff() {
		isTakingOff;
	}
	/*
	unit Update() {
		if this.isTakingOff {
			number airspeed = (number)_legacy(Vessel:Airspeed);
			extern "KOS" {
				print airspeed.
			}
			
			if airspeed > 60 {
				
			}
		}
	}*/
}